name: tenis_minimatch
description: A new Flutter project.
publish_to: 'none'
version: 0.1.4

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  audioplayers: ^5.2.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  wakelock: ^0.6.2

dev_dependencies:
  flutter_launcher_icons: "^0.13.1"
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/sound/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/icon.png"
  min_sdk_android: 26
